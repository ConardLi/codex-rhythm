## What changed

Describe the user-visible change and why it is needed.

## Verification

- [ ] `./scripts/test.sh` passes
- [ ] `./scripts/build.sh` passes
- [ ] Changed UI was checked in both the popover and standalone window
- [ ] No credentials, account IDs, or private logs are included

## Security and compatibility

Does this change affect network requests, credential access, local process execution, persistence, or supported macOS versions?
